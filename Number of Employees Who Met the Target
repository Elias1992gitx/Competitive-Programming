class Solution:
    def numberOfEmployeesWhoMetTarget(self, hours: List[int], target: int) -> int:
        employees = 0
        for i in range(0 ,len(hours)):
            if hours[i] >= target:
                employees += 1
        return employees
